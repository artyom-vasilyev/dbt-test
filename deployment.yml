dbtVersion: 0.19.0
jobs:
  - name: test
    schedule: '0 */6 * * *'
    steps:
      - name: run models
        command: dbt run
  - name: S            P                 A                    C                     E
    schedule: '0 */12 * * *'
    steps:
      - name: version
        command: dbt --version
  - name: ファイヴトラン
    schedule: '0 */12 * * *'
    steps:
      - name: version
        command: dbt --version